func println(int msg) {
    int yep = msg + 15;
    int nope = yep + msg;
}

func test() {
    int sex = 42 / 2;
    println(sex);
}

func main(int argc, long argv) {
    test();
    test();
    test();

    int no = 2;
}